<div class="bg-dark text-light">
  <div class="container">
    <div class="d-flex">
      <div class="col-md-12">
        <h1 class="fw-bold">Tech Blog</h1>

      </div>
    </div>
  </div>
</div>

<div class="container" name="image" id="image" enctype="multipart/form-data">
  {{#each posts as |post|}}
  {{>post-info post=post active_home=true}}
  {{/each}}
</div>